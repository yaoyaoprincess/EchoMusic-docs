---
title: 管理与安全
---

# ⚙️ 管理与安全

## 管理插件

| 操作 | 说明 |
|------|------|
| **启用/禁用** | 点击插件卡片上的开关即可切换。禁用时会调用插件的 `deactivate` 清理注册的资源 |
| **卸载** | 点击删除按钮，会同时删除插件目录、清除数据和故障记录 |
| **设置** | 部分插件提供设置面板，点击齿轮图标进入配置 |
| **更新** | 在线安装的插件，可在插件管理中一键更新到最新版本 |

## 安全模式

EchoMusic 提供全局「**插件安全模式**」，开启后不会加载任何插件，但保留每个插件的启用状态。

### 何时使用安全模式

- 某个插件导致界面卡死或崩溃
- 启动后窗口无响应
- 排查插件兼容性问题

### 进入安全模式

**方式一：插件管理页**

在「插件管理」中打开「插件安全模式」开关，然后重启应用。

**方式二：命令行启动**

```bash
EchoMusic --safe-mode
```

开发环境下：

```bash
pnpm exec electron . --safe-mode
```

### 自动触发

EchoMusic 会记录插件启动和运行状态。如果某个插件导致渲染进程异常退出，主进程会自动尝试切换到安全模式并重载窗口；如果应用被强制关闭或渲染进程无响应，下次启动时也会自动进入安全模式。

插件管理页会在对应插件卡片上用 ⚠️ 警告标记展示启动失败、运行异常或最近一次疑似故障；点击后可查看异常来源、时间、消息和堆栈，也可以清除该插件的异常记录。

## 故障排查

如果遇到插件相关问题：

1. **启用安全模式**：先进入安全模式确认是插件导致的问题
2. **逐一排查**：关闭安全模式后逐个启用插件，找出问题插件
3. **查看异常记录**：插件管理页会用 ⚠️ 标记异常插件，点击可查看异常来源、时间、消息和堆栈
4. **清除记录**：如果问题已解决，可以清除异常记录

::: warning 注意
插件运行在渲染进程中，具有较高权限。请只安装来源可信的插件。如果插件来源不明，建议先阅读其代码再安装。
:::

## 命令行参数

| 参数 | 说明 |
|------|------|
| `--safe-mode` | 以安全模式启动，不加载任何插件 |

---

- [← 插件列表](./list)
- [插件系统概览 →](./)
- [🔌 插件开发文档 →](/develop/plugin-dev/)
